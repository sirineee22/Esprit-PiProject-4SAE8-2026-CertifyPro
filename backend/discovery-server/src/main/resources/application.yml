server:
  port: 8761

eureka:
  client:
    register-with-eureka: false   # ✅ correct for the server itself
    fetch-registry: false         # ✅ correct
  server:
    wait-time-in-ms-when-sync-empty: 0  # ✅ good for dev