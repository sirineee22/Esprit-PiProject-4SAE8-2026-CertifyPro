server:
  port: 8082

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:4200"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
      discovery:
        locator:
          enabled: true
      routes:
        - id: user-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/users/**,/api/roles/**
          filters:
            - AuthenticationFilter
        - id: auth-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/auth/**
        - id: trainer-requests-service
          uri: lb://USER-SERVICE
          predicates:
            - Path=/api/trainer-requests/**
          filters:
            - AuthenticationFilter
        - id: evaluations-service
          uri: lb://TRAINING-SERVICE
          predicates:
            - Path=/api/evaluations/**,/api/evaluations
          filters:
            - AuthenticationFilter
        - id: formations-service
          uri: lb://TRAINING-SERVICE
          predicates:
            - Path=/api/formations/**,/api/formations
          filters:
            - AuthenticationFilter
        - id: quizzes-service
          uri: lb://TRAINING-SERVICE
          predicates:
            - Path=/api/quizzes/**,/api/quizzes
          filters:
            - AuthenticationFilter
        - id: progression-service
          uri: lb://TRAINING-SERVICE
          predicates:
            - Path=/api/progression/**,/api/progression
          filters:
            - AuthenticationFilter
        - id: favorites-service
          uri: lb://TRAINING-SERVICE
          predicates:
            - Path=/api/favorites/**,/api/favorites
          filters:
            - AuthenticationFilter
        - id: stats-service
          uri: lb://TRAINING-SERVICE
          predicates:
            - Path=/api/stats/**,/api/stats
          filters:
            - AuthenticationFilter

eureka:
  client:
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://localhost:8761/eureka/
